name: data_engineering_poc
version: "1.0.0"
config-version: 2

profile: data_engineering_poc

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_engineering_poc:
    staging:
      +materialized: view
      +schema: staging_dbt
    analytics:
      +materialized: view
      +schema: analytics
